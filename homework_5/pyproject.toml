[project]
name = "homework-5"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "redis",
    "fakeredis",
    "requests",
    "pytest",
    "pytest-mock"
]
